# Local dev env. In production these are injected by the deploy workflow.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/slopfeed
JWT_SECRET=dev-secret-change-me
INTERNAL_SECRET=dev-internal-secret
PORT=3000
APP_ENV=dev
# Optional: override the seeded admin password (default: slopfeed-start-2026)
# ADMIN_PASSWORD=...
# ADMIN_RESET=true
